
// SETTINGS :

export const settings : any = {
    defaultEditorTabSize : 4 as number,
    defaultEditorLineHeight : 20 as number,
    defaultEditorWidth : "100%" as string,
    defaultEditorHeight : "100%" as string,
    defaultEditorFont : "monospace" as string,
    defaultEditorFontSize : 12 as number,
    defaultEditorWordSpacing : 0 as number,
    defaultEditorLang : "javascript" as string,
    defaultEditorTheme : "monaco" as string

};